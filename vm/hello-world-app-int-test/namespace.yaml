---
- hosts: localhost
  tasks:
  - name: create a user
    ansible.builtin.user:
        name: dev-hello-world-app-int-test
  - name: cope platform configs
    ansible.builtin.copy:
        src: ./platform-config.sh
        dest: /home/dev-hello-world-app-int-test/platform-config.sh
        owner: dev-hello-world-app-int-test
        group: dev-hello-world-app-int-test
        mode: u+rw,g-wx,o-rwx